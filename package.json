{
  "name": "testing",
  "version": "1.0.0",
  "description": "Testing automation scenarios",
  "main": "index.js",
  "scripts": {
    "postinstall": "npx playwright install",
    "test": "npx playwright test",
    "test-headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.5"
  }
}